# modbus-emu

У сенсора должна быть:
* 0 Coil для управления - on/off
* 1 Coil для сброса состояния
* 0 Input Register для статуса: 0 = Normal, 1 = Alarm (T > 100°C), 2 = Defect (обрыв датчика).
* 1 Input Register для значения температуры/задымлённости/давления и т. п.

Нужно сделать иерархию:
* Sensor -> ThermalSensor и у термального сенсора свои доп регистры
* Sensor -> SmokeSensor + smoke reigsters
* Sensor -> 


TODO:
* Переделать UI чтобы можно было на сенсорах выставлять значения регистров, включать выключать, менять статус
* Описать то, что хочешь от UI и попросить нейронку накатать варианты